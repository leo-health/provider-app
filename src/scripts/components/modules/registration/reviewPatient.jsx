var React = require('react');

module.exports = React.createClass({
  handleOnSubmit: function(e){
    e.preventDefault();
  },

  render: function(){
    return(
      <div>

      </div>
    )
  }
});
