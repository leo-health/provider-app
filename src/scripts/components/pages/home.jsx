var React = require('react');

var HomeHeader = require('./homeHeader');

module.exports = React.createClass({
  render: function() {
    return (
      <div>
        <HomeHeader/>
      </div>
    )
  }
});
