@css {
  .recipient-field-container {
    width: 100%;
    height: 60px;
    box-shadow: 0 1px 4px rgba(0, 0, 0, 0.3);
  }

  .messenger-reply {
    line-height: 20px;
    height: 50px;
  }

  .find-family-field {
    line-height: 40px;
  }
}
